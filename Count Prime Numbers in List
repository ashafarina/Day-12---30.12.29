nums = list(map(int, input().split()))
count = 0

for n in nums:
    if n > 1:
        for i in range(2, n):
            if n % i == 0:
                break
        else:
            count += 1

print("Prime count:", count)
